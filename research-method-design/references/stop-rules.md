See the shared stop rules at `../../shared/references/stop-rules.md`.

Skill-specific stop rules:

- trim the live mechanism set aggressively
- route back to problem formulation if a core mechanism assumption invalidates the framing
