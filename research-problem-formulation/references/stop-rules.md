See the shared stop rules at `../../shared/references/stop-rules.md`.

Skill-specific stop rules:

- force scope cut if repeated reframing does not reduce the hypothesis set
- stop before method design once the evidence plan is clear
