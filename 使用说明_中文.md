# 博士学习 Skill 使用说明

本文档给出这套个人 skill 的中文使用流程、一个完整示例，以及一轮自测时应保存的产物。

## 这套 skill 的目标

目标不是“让 AI 替你完成思考”，而是：

- 让 agent 帮你加速搜索、补洞、批判、整理
- 把第一次建模、最终决策、独立复述和迁移保留给你自己

如果一次使用结束后，你只是拿到了一个答案，但自己不能复述、不能解释为什么这么选、不能把它迁移到下一题，那么这次使用就不算成功。

## 四个 skill 的分工

### 1. `targeted-knowledge-closure`

适用场景：

- 某个概念、方法、公式、系统机制卡住了当前任务
- 你需要快速补齐一个知识点，但不想变成被动看解释

输出重点：

- `knowledge-closure-note.md`
- `transfer-check.md`

### 2. `engineering-task-decomposition`

适用场景：

- 接到一个工程需求，但不清楚系统现状
- 不知道先看哪里、先查什么、先做哪一步

输出重点：

- `system-snapshot.md`
- `unknowns-checklist.md`
- `solution-options.md`
- `execution-slice-plan.md`
- `decision-record-engineering-path.md`

### 3. `research-problem-formulation`

适用场景：

- 有研究直觉，但问题还不够清楚
- 知道可能重要，但重要性、边界、现有工作失效原因还没钉住

输出重点：

- `problem-card.md`
- `failure-taxonomy.md`
- `evidence-gap-list.md`
- `decision-record-problem-scope.md`

### 4. `research-method-design`

适用场景：

- 研究问题已经基本清晰
- 缺的是方法设计、机制选择、失败模式和最小验证计划

输出重点：

- `design-card.md`
- `mechanism-comparison.md`
- `failure-mode-table.md`
- `minimal-validation-plan.md`
- `decision-record-method-choice.md`

## 通用使用流程

每次都严格按下面这 8 步走：

1. 先判断主 skill 是哪一个。
2. 先写最小输入，不允许一开始就让 agent 全包。
3. 明确这轮 agent 的单一角色。
4. 产出本轮 artifact。
5. 到分叉处必须由你写 decision record。
6. 如果被知识点卡住，插入 `targeted-knowledge-closure` 子流程。
7. 做一次脱离 agent 的完成检查。
8. 保存本轮产物，便于复盘和后续测试。

## 最小输入模板

### 学知识

- 我认为这个概念是：
- 它卡住我的地方是：
- 我最困惑的是：

### 拆工程

- 我理解这个需求是：
- 可能影响的系统部分是：
- 我现在不知道的是：

### 定义科研问题

- 我怀疑的问题是：
- 我觉得它重要是因为：
- 我觉得现有工作有一个关键问题是：

### 设计科研方法

- 我要解决的问题是：
- 一个候选机制是：
- 我不放心它的原因是：

## 一个完整流程例子

下面用 `engineering-task-decomposition` 举例。

### 场景

需求来了：

“给评测系统加一个导出报告功能。”

你现在只知道：

- 可能和 API、前端、报告数据生成有关
- 但不知道报告数据是在哪组装的
- 也不知道系统是否已经有部分导出能力

### 第一步：你先写最小输入

```md
- 我理解这个需求是：给评测系统增加报告导出功能
- 可能影响的系统部分是：API、报告 UI、报告数据拼装流程
- 我现在不知道的是：报告数据在哪里生成、是否已有导出接口、导出格式要求是什么
```

### 第二步：agent 只当 clarifier

这轮只让 agent 做一件事：

- 生成 unknowns checklist
- 提出先验证哪些仓库事实

这轮不允许它直接给最终方案。

### 第三步：你去看证据

例如检查：

- 报告页面入口文件
- 后端报告 API
- 是否已有导出相关路由/按钮/服务
- 配置项和日志

并把结果写进 `system-snapshot.md`，同时标记：

- observed
- inferred
- unknown

### 第四步：agent 只当 system-mapper

让 agent 基于你已经补进去的证据：

- 帮你画清边界
- 把任务拆成 2 到 3 个实现路径
- 指出每条路径依赖什么前提

### 第五步：你写 decision record

你自己必须写：

- 为什么选这条
- 为什么不选另外两条
- 什么证据会让我改选

### 第六步：agent 只当 design-reviewer

让 agent 检查：

- 风险有没有漏
- 第一个 execution slice 是否足够小
- 验证顺序是否合理

### 第七步：脱离 agent 检查

你自己回答，不看 agent：

- 这个需求在系统里到底意味着什么
- 当前边界在哪里
- 我先做哪一个最小切片
- 为什么先做它

如果说不出来，这轮还没真正完成。

## 插入式学习子流程

当你在科研或工程流程里被某个知识点卡住时，不要重开一个大任务，直接插入一个短子流程：

1. 写下你当前对这个概念的理解。
2. 让 agent 只纠错，不先长篇解释。
3. 让 agent 给直觉版、形式化版、任务内版解释。
4. 做一个 near transfer。
5. 回到主流程继续推进。

这个子流程的原则是：

- 主流程不断
- 学习补洞尽量短
- 学完立刻回到原任务里用

## 自测时怎么做

每个 skill 至少用一个真实或半真实样例跑一次，并记录：

- 入口是否够轻
- agent 有没有串角色
- artifact 是否完整
- 工程类有没有证据锚点
- decision 是否真由你写
- 最后能否脱离 agent 复述

建议把每次自测都写成一个 `run-summary.md`。
